{
  "name": "telegram-sudoku",
  "version": "1.0.0",
  "private": true,
  "type": "module",

  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },

  "dependencies": {
    "@cf-wasm/resvg": "^0.4.2",
    "sudoku-core": "3.0.3"
  },

  "devDependencies": {
    "wrangler": "^4.114.0"
  }
}
